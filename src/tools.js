// mouse tools


// chop wood tool

function ChopWoodTool() {
    this._active = true;
    this._map = null;

    // click to select region

    this.chopTreesInRegion = function(upperLeft, lowerRight) {
        /*
        var tilesInRegion = this._map.tilesInRegion(upperLeft, lowerRight);
        for(var i = 0; i < tilesInRegion.length; i++) {
            var tile = tilesInRegion[i];
            var entities = tile.entities;
            for(var entityIndex = 0; )
        }
    */
    };
};


// create stockpile tool

