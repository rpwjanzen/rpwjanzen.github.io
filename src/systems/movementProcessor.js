// Movement processor
// Move each item if possible by amount
Crafty.c('MovementProcessor', {
   update: function() {
   		// for each entity with Position and Velocity, entity.Position += Velocity
   } 
});
